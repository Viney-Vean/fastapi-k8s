apiVersion: v1
kind: Service
metadata:
  name: fastapi
  labels:
    app: fastapi
spec:
  ports:
    - port: 8080
      targetPort: 8080
      nodePort: 30010
      protocol: TCP
      name: http
  selector:
    app: fastapi
  type: LoadBalancer   # MetalLB will assign external IP
